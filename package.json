{
  "engines": {
    "node": ">=14.0.0"
  },
  "type": "module",
  "scripts": {
    "dev": "yarn run ci && pnpm -C packages/apps run dev",
    "build_github": "yarn run ci && pnpm -C packages/apps run build_github",
    "deploy": "yarn run build_github && gh-pages -d build && git checkout HEAD -- packages/apps/src/public/thanhlv-vi-cv.pdf",
    "deploy-gh-pages": "gh-pages -d build --repo https://x-access-token:${GITHUB_TOKEN}@github.com/thanhlv-com/thanhlv-blog.git",
    "ci": "npm i -g pnpm && pnpm install"
  },
  "devDependencies": {
    "blog": "workspace:*",
    "@vue/theme": "workspace:*",
    "gh-pages": "6.1.1"
  },
  "pnpm": {
    "overrides": {
      "@vitejs/plugin-vue": "5.0.0-beta.1",
      "blog": "workspace:*"

    },
    "peerDependencyRules": {
      "ignoreMissing": [
        "@algolia/client-search",
        "react",
        "react-dom",
        "@types/react",
        "search-insights"
      ]
    }
  }
}
